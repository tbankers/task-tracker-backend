services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: notes
    ports:
      - "8080:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
