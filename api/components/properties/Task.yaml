type: object
properties:
  id:
    type: integer
  column_id:
    type: string
    format: uuid
  name:
    type: string
  description:
    type: string
    nullable: true
  assigned_id:
    type: string
    format: uuid
    nullable: true
  created_by:
    type: string
    format: uuid
    nullable: true
  created_at:
    type: string
    format: date-time
  updated_at:
    type: string
    format: date-time
  blocked_by:
    description: List of task IDs that must be completed before this task
    type: array
    items:
      type: integer
